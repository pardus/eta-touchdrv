2026-04-23  Vrdons <vrdons@proton.me>
	* touch2, touch4: apply -O3 optimization
	* touch2/kernelSrc/OpticalDrv.c: Keep device state alive across
	open file handles until disconnect and release both complete.

2026-04-22  Vrdons <vrdons@proton.me>

	* touch2/kernelSrc/OpticalDrv.c: Fix probe null dereference and
	avoid input device double free on teardown.
	* touch2/kernelSrc/OpticalDrv.c: Clamp interrupt URB copies to the
	fixed receive buffer size.
	* touch4/kernel/OtdDrv.c: Initialize GET_REPORT request ID from user
	data instead of uninitialized heap memory.
	* touch4/kernel/OtdDrv.c: Fix probe failure cleanup and prevent
	char-device use-after-free across USB disconnect.
	* touch2/kernelSrc/Makefile, touch4/kernel/Makefile: Use KVER-aware
	Kbuild module targets for Debian/Pardus DKMS builds.
	* touch2/kernelSrc/OpticalDrv.c, touch4/kernel/OtdDrv.c: Add missing
	string declarations for GCC/clang kernel builds.
	* Code formatter

2015-12-06  Gokhan Karabulut <gokhan.karabulut@tubitak.gov.tr>

	* README.md, touch4/kernel/OtdTouchDriver.c: Solve ohci-hcd problems
	of AMD IWBs This commit solves ohci-hcd problems of AMD Interactive White
	Boards.  Driver works fine with ehci and xhci, but it fails to keep
	track of touching fingers when it comes to ohci. This commit solves
	this problem implementing a circular queue. Before this commit, the
	driver would get 64 bytes from usb_interrupt_msg and server daemon
	would get this 64-byte chunk through the read system call. By the
	help of a circular queue, the driver gets multiple of 64 bytes and
	sends a single 64-byte chunk to the server when it calls read system
	call.  Fix indentation style and format Update README.md

2015-12-06  Gokhan Karabulut <gokhan.karabulut@tubitak.gov.tr>

	* Initial commit
